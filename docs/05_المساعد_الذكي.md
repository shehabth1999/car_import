# 5. المساعد الذكي (KA Sales) — بيشتغل إزاي

> وركفلو **واحد** لكل الأرقام والقنوات. النسخ القديمة (آية / رامي / سوشيال) اتوقفت يوم 2026-09-17
> ومحفوظة عشان سجل تنفيذها.

## 5.1 الرسمة: أربع محطات بس

```
       رسالة العميل
            │
            ▼
   ① prepare_turn  (Python — من غير موديل)
       • يجيب صفقة العميل من الداتابيز
       • يكتب "الحقائق": رقم الصفقة، المرحلة، الباخرة، ETA، حالة الدفع
       • يكتب "التحذيرات" حسب الرسالة دي بالذات:
            - فيها كلام فلوس؟         → حوّل لزميل
            - فيها كوريا؟             → حوّل لزميل
            - فيها تقسيط/مصاريف؟      → حسب مفتاح الإفصاح
            - مفيهاش أي حاجة من دول؟  → جاوب بنفسك ومتحوّلش
       • يقرر: في كلام يستاهل موديل أصلاً؟ (needs_ai)
            │
            ▼
   ② gate_needs_ai  (شرط)
            ├── لأ ──► ③ handled_in_python (يسجّل وخلاص، مفيش رد)
            │
            └── آه ─► ④ sales_agent
                         • الموديل + التعليمات + الحقائق + التحذيرات
                         • ومعاه 13 أداة ينده عليها + مكتبة الإجابات المعتمدة (RAG)
                         │
                         ▼
                      الرد للعميل
```

**الفكرة الأساسية:** Python بيقرر كل حاجة ممكن تتحسب، والموديل بيقرأ المعنى بس.
ده بيخلي السلوك ثابت ومتوقّع، وبيوفّر فلوس (لأن الرسايل الفاضية مش بتوصل للموديل
أصلاً).

---

## 5.2 التعليمات — رسالتين للموديل

الموديل بيقرا **رسالتين نظام** كل مرة:

```
① ثابتة (بتتخزّن في الكاش، ما بتتغيّرش):
   core.md          القواعد اللي متتكسرش
   lane_sales.md    إمتى ينده على كل أداة
   voice.md         الصوت الواحد (وبيتأقلم مع القناة)

② ديناميكية <dynamic_context> (بتتبني كل رسالة، من غير كاش):
   دلوقتي، القناة (واتساب/ماسنجر/…)
   العميل: الاسم، الرقم، بلد الإقامة، حالة المبادرة، الميزانية،
           والتأهيل من آخر Lead (البرنامج، العربية المطلوبة، الميزانية، الأهلية)
   الصفقة الحالية: الرقم، المرحلة، الباخرة، ETA، حالة الدفع
   ملخص المحادثة (بيتعمل تلقائياً كل 25 رسالة)
   آخر 6 رسايل (سطر لكل واحدة)
   تحذيرات خاصة بالرسالة دي
   إرشادات الإدارة + إرشادات خاصة بالرقم/الحساب + إرشادات الإعلان اللي جاب العميل
```

نفس الشكل اللي شغال على المساعدين التانيين في الشركة (معالم، البسمة). البيانات بتتبني في
`services/turn_context.py` (لأن صندوق كود العقدة محدود بـ10,000 حرف).

**عايز تغيّر الأسلوب؟** عدّل `prompts/voice.md`، ارفع، أعد بناء الوركفلو، وأعد تشغيل Celery.
**عايز توجيه لرقم واحد بس؟** خانة *instructions* في إعدادات حساب الواتساب — من غير وركفلو تاني.
**عايز تضيف توجيه سريع من غير مبرمج؟** استخدم "إرشادات الإدارة" من AI Studio.

---

## 5.3 صوت واحد لكل القنوات

مفيش اسم موظف. «فريق مبيعات خالد أوتوموبيل». الملف `prompts/voice.md` بيجمع: جمل آية القصيرة
و«حضرتك» وسؤال واحد في المرة، ترتيب رامي للمعلومة في نقط وسطر «الخطوة اللي بعدها»، وسطر
التعريف القصير والاختصار على ماسنجر/إنستجرام/تيك توك. المساعد بيعرف القناة من الرسالة
الديناميكية وبيتصرف على أساسها. عند التشغيل: الرقمين على نفس الوركفلو
(`build_ka_workflows --release <account_id>` مرتين).

---

## 5.4 الأدوات (13 أداة — اللي بتخلي المساعد يعرف حاجة حقيقية)

| الأداة | العميل بيسأل إيه | بترجّع إيه |
|---|---|---|
| `ka_get_deal_status` | "وصلت فين؟" | المرحلة، الباخرة، البوليصة، ETA، الميناء |
| `ka_send_deal_status_update` | "ابعتلي تحديث مكتوب" | **بتبعت** الرسالة بنفسها |
| `ka_check_import_eligibility` | "ينفع أستورد كذا؟" | مسموح/ممنوع + السبب + المستندات |
| `ka_get_instalment_plan_terms` | "ينفع أقسط؟" | الشروط (لو الإفصاح مفتوح) |
| `ka_get_fee_and_licensing_costs` | "الرسوم بكام؟" | المصاريف المعتمدة (لو الإفصاح مفتوح) |
| `ka_escalate_conversation_to_staff` | أي حاجة فلوس | **بتقفل المساعد**، تبعت رسالة انتظار، **وتبعت للزميل الأرقام المعتمدة كاقتراح يتأكد منه** |
| `ka_get_document_checklist` | "إيه الورق المطلوب؟" | الناقص بس — ومتطلبش بطاقة في الشات |
| `ka_search_vehicle_listings` | "عندكم C200؟" | العربيات **من غير أسعار** |
| `ka_search_initiative_listings` | "عايز أشتري مبادرة" | المعروض للبيع |
| `ka_register_initiative_for_sale` | "عايز أبيع مبادرتي" | بيتسجل **كمسودّة** لحد ما زميل يتأكد |
| `ka_schedule_followup` | العميل قال هيفكر | تذكير للمسؤول (نشاط الساعة 9)، **من غير ما يبعت للعميل** |
| `ka_log_call_outcome` | (للموظف) | نتيجة مكالمة على الصفقة |
| `ka_share_bank_details` | "ابعتلي رقم الحساب" | **بترفض** — إلا لو فيه طلب موافقة إدارة مسجّل ومعتمد |
| `ka_file_customer_document` | العميل بعت صورة البطاقة | بتحفظها على المستند الصح في الصفقة |

الأداة اللي بتبعت **بتبعت بنفسها**، والمساعد مأمور يسكت بعدها. لو لقيت المساعد
بيكتب "تم إرسال التحديث" بعد ما الأداة بعتت — دي مشكلة تتصلّح في `lane_sales.md`.

---

## 5.5 المزوّد والموديل والفلوس

**الوضع الحالي:**
- الموديل الأساسي: `claude-haiku-4-5-20251001` (Anthropic)
- الموديل الاحتياطي: `gpt-4.1-2025-04-14` (OpenAI)
- المفتاح: **من السيرفر المركزي**، للاختبار بس

**ملحوظتين مهمتين:**

1. **اتغيّر لـ Haiku 4.5 بقصد.** المساعد بيعمل توجيه ونداء أدوات، مش تحليل
   معقّد — لأن Python بتقرر كل حاجة ممكن تتحسب قبل ما الموديل يشتغل أصلاً.
   Opus كان بيتحسب على كل رسالة عميل من غير داعي.
2. **لازم يكون في موديل احتياطي من مزوّد تاني.** لو المزوّد وقع ومفيش احتياطي،
   رسالة الخطأ بتتحوّل لرد المساعد نفسه. عندنا احتياطي، بس الاتنين كانوا من غير
   مفتاح في الأول — والنتيجة كانت إن المساعد "رد" برسالة خطأ إنجليزية.

---

## 5.6 قبل ما توصّله برقم حقيقي — الترتيب الإجباري

1. **مفتاح المزوّد بتاع العميل** يتحط (مش بتاع الشركة الأم).
2. **اتأكد من الرصيد**: `ai.monthly_contact_limit` و `ai.paid_until`.
3. **جرّب على أرقام الموظفين الأول** (canary): بتربط الوركفلو بجهة اتصال
   واحدة أو اتنين بس، مش بالرقم كله.
4. **سيبه أسبوع** وراجع المحادثات: رد غلط؟ رقم اخترعه؟ حوّل من غير سبب؟
5. **بعدين** اربطه بالرقم الحقيقي.
6. **زرار الطوارئ:** أمر `build_ka_workflows --rollback` بيفك الربط من كل
   الأرقام وكل العملاء فوراً — البشر بياخدوا كل حاجة.

---

## 5.7 إزاي تراجع شغل المساعد

| عايز تشوف | روح فين |
|---|---|
| كل تشغيلة حصلت وبياناتها | AI Studio → Executions (`/genie/26/`) |
| رسمة الوركفلو ونصوصه | AI Studio → Workflows (`/genie/24/`) |
| الأدوات المسجّلة | AI Studio → Tools (`/genie/30/`) |
| المزوّدين ومفاتيحهم | AI Studio → Providers (`/genie/33/`) |
| المحادثات نفسها | `/chat` |

---

## 5.8 سلوك اتكشف بالتجربة، تعرفه قبل ما يفاجئك

| السلوك | التفسير |
|---|---|
| المساعد حوّل محادثة وبعدين سكت خالص | ده مقصود. مفيش حاجة بترجّع المحادثة للمساعد غير إنسان. **لازم يكون في حد بيراجع المحوّل.** |
| رد فاضي بعد ما أداة بعتت | مقصود — الأداة بعتت، والمساعد مأمور يسكت |
| كان بيحوّل أسئلة عادية بعد ما زميل يرجّعله المحادثة | كانت غلطة واتصلحت: تحذير "الصفقة مش مدفوعة" كان بيتقال كل رسالة وكان بينتهي بـ"حوّل لزميل" |
| قال قايمة الأسعار وهو المفروض مقفول | كانت غلطة واتصلحت: النهارده الأداة نفسها بترجّع صفر أرقام وهي مقفولة |

---

## 5.9 سؤال الفلوس — إيه اللي بيحصل بالظبط

قرار العميل يوم 2026-09-16: *"بفضل يبقى مقفول في الاول مع اضهار اقتراح لمطابقة
الارقام هل هي صحيحة ام لا"*. يعني حاجتين مش واحدة:

1. **المساعد يفضل مقفول.** لما العميل يسأل "الرسوم بكام؟"، المساعد **مبيقولش أي
   رقم** وبيحوّل لزميل. ده كان شغال من الأول.
2. **الزميل يشوف الأرقام كاقتراح يتأكد منه — جوه المحادثة نفسها.** ده الجديد.
   أول ما التحويل يحصل، بتتكتب **ملاحظة داخلية** في الشات نفسه (نوع رسالة
   `internal`، العميل **عمره ما هيشوفها** ومستحيل تتبعت لواتساب) وفيها منشن
   للمندوب المسؤول، وفيها:
   - آخر عرض سعر للعميل (الإجمالي، المقدم، الباقي، المدفوع، ولو دفع زيادة)
   - كل المصاريف المعتمدة في النظام
   - شروط التقسيط المعتمدة
   - **وفوق كله سطر: "❗ تأكد إنها لسه صح قبل ما تقولها للعميل"**

   والإشعار العادي بيتبعت كمان، بس **الملاحظة هي الأساس**: الإشعار بيقولك
   "في حد محتاجك" وبعدين يوديك تدوّر على السبب؛ الملاحظة بتقوله لك في المكان
   اللي هتكتب فيه الرد، وبتفضل موجودة بكرة لأي حد يفتح المحادثة بعد كده.

الفرق مش شكلي. الزميل اللي لازم يروح يدوّر على الأرقام يا إما مش بيدوّر، يا إما
بيلاقي نسخة تانية منها. الزميل اللي شايفها قدامه مع سؤال "دي لسه صح؟" يا إما
بيأكد في ثانية، يا إما **بيمسك رقم قديم قبل ما يوصل العميل** — وده أصلاً سبب إن
المفتاح مقفول.

**كل تحويل بيسيب ملاحظة**، مش بس اللي فيه فلوس — الزميل اللي بيفتح المحادثة
لازم يعرف هي جاتله ليه قبل ما يقرا كلام العميل. بس **الأرقام بتتحط في
مواضيع الفلوس بس**: شكوى عن خدش في الصدام مش محتاجة جدول المصاريف متعلّق فيها.

نفس الحكاية في **متابعة المحادثات المنسية**: لما الشغّالة تلاقي عميل محوّل
ومستني من غير رد، بتكتب ملاحظة بالمنشن في المحادثة نفسها كمان، مش إشعار
وبس.

### ولو فيه رقمين بنفس القيمة؟
الإشعار **بيقولها بصوت عالي**. دلوقتي مثلاً 4,750 € مكتوبة مرتين — مرة "أتعاب
الشركة" ومرة "مصاريف الشحن" — و 55,000 جنيه مكتوبة "الميناء والتخليص" و "مصاريف
ميناء الإسكندرية". الإشعار بيسأل: أنهي واحد الصح؟ ده بالظبط "الاقتراح لمطابقة
الأرقام" اللي العميل طلبه، والزميل اللي على وشك يقول واحد منهم هو أنسب واحد
يتسأل.

### المواضيع اللي بتطلّع الإشعار ده
`money` · `discount` · `refund` · `instalment_amount` · `price` · `fees`

شكوى عن خدش في الصدام مش محتاجة جدول المصاريف متعلّق فيها.


---

## 5.10 بوابة الرد الصادرة — آخر خط دفاع

`patches.py` بيركّب دالة قدام `execute_workflow_sync` (اللي كل القنوات بتنده عليها). بعد ما
الموديل يكتب رده وقبل ما يوصل العميل، `services/supervisor.gate` بيفحصه بخمس قواعد
(رقم مش راجع من أداة، بيانات بنكية، تأكيد تحويل، وعد بتاريخ، رد فاضي…):

- **تحذير** ← الرد بيتبعت، وملاحظة داخلية للمندوب بالسبب.
- **إيقاف** ← العميل بيوصله جملة انتظار، المحادثة بتتحوّل لموظف، والرد الموقوف بيتكتب
  في ملاحظة داخلية مع إحاطة الفلوس عشان الموظف يكمّل.

مفيش رد فاضي أبداً: الرد الفاضي بيخلي المنصة تعيد تشغيل الموديل وتبعت إيميل عطل.
